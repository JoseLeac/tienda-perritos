name: CI/CD Tienda Perritos - Backend

on:
  push:
    branches: ["main"]
    paths:
      - "backend/**"

jobs:
  build-push-deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      # ==========================================
      # CONFIGURACIÓN DE CREDENCIALES (AWS Y DOCKER)
      # ==========================================
      - name: Configurar credenciales AWS
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Login en Amazon ECR (runner)
        uses: aws-actions/amazon-ecr-login@v1

      - name: Login en Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # ==========================================
      # CONSTRUCCIÓN Y DOBLE DISTRIBUCIÓN
      # ==========================================
      - name: Build imagen Backend y push (ECR + Docker Hub)
        run: |
          cd backend

          # 1. Construir la imagen base localmente en el runner
          docker build -t tienda-backend .

          # 2. Etiquetar y subir a Amazon ECR (Tu entorno privado en AWS)
          docker tag tienda-backend:latest ${{ secrets.ECR_REPO_URL_BACKEND }}:latest
          docker push ${{ secrets.ECR_REPO_URL_BACKEND }}:latest

          # 3. Etiquetar y subir a Docker Hub (Tu repositorio público leaduran/tienda-backend)
          docker tag tienda-backend:latest ${{ secrets.DOCKERHUB_USERNAME }}/tienda-backend:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/tienda-backend:latest

      # ==========================================
      # DESPLIEGUE EN EL SERVIDOR (EC2)
      # ==========================================
      - name: Desplegar Backend vía SSM
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.EC2_BACKEND_INSTANCE_ID }} \
            --document-name "AWS-RunShellScript" \
            --comment "Actualizar tienda-backend desde ECR" \
            --parameters '{"commands":[
              "set -e",
              "aws ecr get-login-password --region '${{ secrets.AWS_REGION }}' | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}",
              "docker pull ${{ secrets.ECR_REPO_URL_BACKEND }}:latest",
              "docker stop tienda-backend || true",
              "docker rm tienda-backend || true",
